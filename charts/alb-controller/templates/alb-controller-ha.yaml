apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: alb-controller-pdb
  namespace: {{ include "albController.namespace" . }}
  labels:
    app: alb-controller
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: alb-controller
